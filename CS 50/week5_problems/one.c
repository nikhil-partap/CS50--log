#include<stdio.h>
#include<cs50.h>

typedef struct node {
    int value;
    struct node *next;
} node;
char list[50];
int main (void){
    

    for (int i = 0; i < 5; i++)
    {
    node *one = malloc(sizeof(node));

    
    node one = {1, NULL};
    
    list = &;
    }
    



}
